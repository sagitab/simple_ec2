# Local .terraform folders
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude any .tfvars files, as they often contain sensitive information
# If you have specific variables that are safe to share, you can whitelist them
*.tfvars
*.tfvars.json

# Ignore override files as they are usually personal to the developer's environment
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include plugin cache directory
.terraform.lock.hcl

# Ignore CLI configuration files
.terraformrc
terraform.rc